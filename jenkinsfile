#!/usr/bin/env groovy
@Library('grovvy_shared_library@main') _ 
node{
              stage('Source') {
               		git 'https://github.com/tvanantha/webapp.git'
                }

              stage('maven build'){
                  steps{
                      script{
		    	       sh "mvn clean install"
                      	  }
               	     }  
                 }	
                 
                 stage ('Check logs') {
                    steps {
                        filterLogs ('WARNING', 1)
                    }
                }
}
