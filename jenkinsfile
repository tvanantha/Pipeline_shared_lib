#!/usr/bin/env groovy
@Library('grovvy_shared_library@main')_ 
node{
  
   
              stage('Source') {
                z= new git_url()
                z.checkOutFrom('tvanantha/webapp.git')
               		
              }
              stage('Build') {
                     bat "mvn package"
               }	
               stage ('Check logs') {
                       filterLogs ('WARNING', 1)
              }
   }
